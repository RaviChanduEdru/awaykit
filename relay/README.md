# awaykit relay (optional)

Self-hostable, zero-knowledge relay: WebRTC signaling, ciphertext message
queue, push notification wake-ups. Never sees plaintext.

Not started yet — daemon + LAN mode come first.
